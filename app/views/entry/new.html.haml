.all
  .main
    .main__entry
      =form_for @entry, url:entry_index_path  do |f|
        .main__entry__name
          新規記事の作成
        = f.text_field :title, placeholder: 'タイトル',class:"main__entry__title"
        .main__entry__category
          = f.label :category, 'カテゴリ'
          = f.collection_check_boxes(:category_ids, Category.all, :id, :name) do |category|
            = category.label do
              = category.check_box
              = category.text

        .main__entry__image
        = f.label :image, class: "main__entry__image__icon" do
          = fa_icon "image",class: 'icon'
          = f.file_field :image, class: "main-form__image--default"
        =f.text_area :text, placeholder:'ブログ本文',class:"main__entry__message"
        =f.submit 'ブログを投稿する',class: 'send'